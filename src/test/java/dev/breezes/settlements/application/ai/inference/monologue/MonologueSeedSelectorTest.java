package dev.breezes.settlements.application.ai.inference.monologue;

/**
 * Seed selection is now covered by {@link MonologueSeedProjectorTest}.
 * <p>
 * {@link MonologueSeedSelector} remains in the codebase for reference but is no longer used
 * in production paths; the projector replaced it. This placeholder prevents a missing-class
 * compile error if the build tool tries to run the old test class by name.
 */
class MonologueSeedSelectorTest {
    // All tests migrated to MonologueSeedProjectorTest.
}
